import T = require('./types');
/**
 * Converts the trivia tokens of "node" to leading trivia (rather than trailing trivia).
 *
 * @param node
 */
export declare function convertToLeadingTrivia(node: T.INode): void;
